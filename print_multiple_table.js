// Calculate the multiple table 

function calculateMultipicationTable ( num , range ){
    for(let i = 1; i <= range ; i++){
        console.log(`${num} * ${i} = ${num * i}`)
    }
    
}

calculateMultipicationTable(6 , 10 )